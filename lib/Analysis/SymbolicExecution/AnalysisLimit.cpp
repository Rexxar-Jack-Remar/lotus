#include "Analysis/SymbolicExecution/AnalysisLimit.h"

// The actual storage for these configurable limits lives in AnalysisState.cpp,
// where each field is bound to an LLVM cl::opt via cl::location(...).
